function addNumbers(a: number, b: number) {
  return a + b;
}

const result: number = addNumbers(2, 2);

console.log(result);

//Devuelve la suma en un string
const addNumbersArrow = (a: number, b: number): string => `${a + b}`;

const resultArrow: string = addNumbersArrow(2, 2);

function multiply( fisrtNumber: number, secondNumber:  ,base){

}

console.log('Resultado flecha:' + resultArrow);
export {};
